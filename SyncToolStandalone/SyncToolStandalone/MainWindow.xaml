<Window x:Class="SyncToolStandalone.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SyncToolStandalone"
        mc:Ignorable="d"
        Title="Ferramenta de Sincronização - Suporte" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Configurações de Conexão -->
        <GroupBox Header="Configurações de Conexão" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Host:" Grid.Row="0" Grid.Column="0" Margin="0,5,5,5"/>
                <TextBox x:Name="TxtHost" Grid.Row="0" Grid.Column="1" Margin="0,5,10,5" Text="localhost"/>

                <Label Content="Porta:" Grid.Row="0" Grid.Column="2" Margin="0,5,5,5"/>
                <TextBox x:Name="TxtPort" Grid.Row="0" Grid.Column="3" Margin="0,5,0,5" Text="3307"/>

                <Label Content="Banco:" Grid.Row="1" Grid.Column="0" Margin="0,5,5,5"/>
                <TextBox x:Name="TxtDb" Grid.Row="1" Grid.Column="1" Margin="0,5,10,5" Text="medusaX8"/>

                <Label Content="Usuário:" Grid.Row="1" Grid.Column="2" Margin="0,5,5,5"/>
                <TextBox x:Name="TxtUser" Grid.Row="1" Grid.Column="3" Margin="0,5,0,5" Text="root"/>

                <Label Content="Senha:" Grid.Row="2" Grid.Column="0" Margin="0,5,5,5"/>
                <PasswordBox x:Name="PwdPass" Grid.Row="2" Grid.Column="1" Margin="0,5,10,5"/>

                <Button x:Name="BtnTest" Content="Testar Conexão" Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" 
                        Margin="0,5,0,5" Padding="10,5" HorizontalAlignment="Right" Click="BtnTest_Click"/>
            </Grid>
        </GroupBox>

        <!-- Botões de Ação -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="BtnSyncClientes" Content="Sincronizar Clientes" Margin="5" Padding="15,5" Click="BtnSyncClientes_Click"/>
            <Button x:Name="BtnSyncProdutos" Content="Sincronizar Produtos" Margin="5" Padding="15,5" Click="BtnSyncProdutos_Click"/>
            <Button x:Name="BtnSyncTudo" Content="Sincronizar Tudo" Margin="5" Padding="15,5" Click="BtnSyncTudo_Click"/>
        </StackPanel>

        <!-- Barra de Progresso -->
        <ProgressBar x:Name="Progress" Grid.Row="2" Height="20" Margin="0,0,0,10" Visibility="Collapsed"/>

        <!-- Log -->
        <GroupBox Header="Log de Operações" Grid.Row="3" Margin="0,0,0,10">
            <TextBox x:Name="TxtLog" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                     FontFamily="Consolas" Background="#F5F5F5"/>
        </GroupBox>

        <!-- Status -->
        <TextBlock x:Name="TxtStatus" Grid.Row="4" Text="Pronto" FontWeight="Bold"/>
    </Grid>
</Window>
