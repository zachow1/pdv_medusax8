<Window x:Class="PDV_MedusaX8.SuprimentoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Suprimento de Caixa" Height="210" Width="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Valor em dinheiro:"/>
        <TextBox Name="TxtValor" Grid.Row="0" Margin="0,25,0,10" Width="160" Text="0,00"/>

        <TextBlock Grid.Row="1" Text="Motivo (opcional):"/>
        <TextBox Name="TxtMotivo" Grid.Row="1" Margin="0,25,0,10"/>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Salvar" Width="100" Margin="0,0,10,0" Click="BtnSalvar_Click" Padding="8,0,8,0"/>
        <Button Content="Cancelar" Width="100" Click="BtnCancelar_Click" Padding="8,0,8,0"/>
    </StackPanel>
    </Grid>
</Window>