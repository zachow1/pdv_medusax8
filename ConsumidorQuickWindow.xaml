<Window x:Class="PDV_MedusaX8.ConsumidorQuickWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Informar Consumidor" Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#FAFAFA">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nome" FontWeight="Bold"/>
        <TextBox x:Name="TxtNome" Grid.Row="1" Margin="0,4,0,10" Height="28"/>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="CPF" FontWeight="Bold"/>
            <TextBox x:Name="TxtCPF" Margin="0,4,0,0" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnSalvar" Content="Salvar" Width="90" Height="30" Margin="0,0,8,0" Click="BtnSalvar_Click" Padding="8,0,8,0"/>
            <Button x:Name="BtnCancelar" Content="Cancelar" Width="90" Height="30" Click="BtnCancelar_Click" Padding="8,0,8,0"/>
        </StackPanel>
    </Grid>
</Window>